# Salin jadi `.env.test` untuk menjalankan integration test (npm run test:integration).
# WAJIB DB TERPISAH dari .env (konterapp_full) -- integration test boleh
# TRUNCATE/DROP data di DB ini kapan saja. Jangan pernah pakai DB yang sama
# dengan dev/production.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/konterapp_full_test"
DIRECT_URL="postgresql://postgres:postgres@localhost:5432/konterapp_full_test"
AUTH_SECRET="test-secret-not-for-production"
